apiVersion: apps/v1
kind: Deployment
metadata: 
  name: hello-version
  namespace: test
spec:
  selector:
    matchLabels:
      run: hello-version
  template:
    metadata:
      labels:
        run: hello-version
    spec:
      containers:
        - name: hello-version
          image: nitikornchumnankul/api-test-version:v1 
          ports: 
            - containerPort: 8080